version: '2'

services:
  db:
    container_name: "minesweeper-db"
    image: postgres:12.3
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: admin
      POSTGRES_USER: postgres
      POSTGRES_DB: postgres
